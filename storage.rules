rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow users to upload CV files to their own folder
    match /users/{userId}/uploads/{jobId}/{fileName} {
      allow read: if request.auth != null && request.auth.uid == userId;
      allow write: if request.auth != null && 
        request.auth.uid == userId &&
        request.resource.size < 10 * 1024 * 1024 && // 10MB limit
        (request.resource.contentType == 'application/pdf' ||
         request.resource.contentType == 'application/vnd.openxmlformats-officedocument.wordprocessingml.document' ||
         request.resource.contentType == 'application/msword' ||
         request.resource.contentType == 'text/csv');
    }
    
    // Also support the old path structure for backward compatibility
    match /users/{userId}/uploads/{fileName} {
      allow read: if request.auth != null && request.auth.uid == userId;
      allow write: if request.auth != null && 
        request.auth.uid == userId &&
        request.resource.size < 10 * 1024 * 1024 && // 10MB limit
        (request.resource.contentType == 'application/pdf' ||
         request.resource.contentType == 'application/vnd.openxmlformats-officedocument.wordprocessingml.document' ||
         request.resource.contentType == 'application/msword' ||
         request.resource.contentType == 'text/csv');
    }
    
    // Allow users to read their generated CVs
    match /users/{userId}/generated/{jobId}/{fileName} {
      allow read: if request.auth != null && request.auth.uid == userId;
      allow write: if false; // Only functions can write
    }
    
    // Allow public access to generated CV HTML files for sharing
    match /users/{userId}/generated/{jobId}/cv.html {
      allow read: if true; // Public access for sharing
      allow write: if false; // Only functions can write
    }
    
    // Allow users to read their generated podcasts
    match /users/{userId}/podcasts/{fileName} {
      allow read: if request.auth != null && request.auth.uid == userId;
      allow write: if false; // Only functions can write
    }
    
    // Public template assets
    match /templates/{allPaths=**} {
      allow read: if true;
      allow write: if false;
    }
    
    // Temporary files for processing (auto-deleted after 24h)
    match /temp/{jobId}/{fileName} {
      allow read: if request.auth != null;
      allow write: if false; // Only functions can write
    }
  }
}